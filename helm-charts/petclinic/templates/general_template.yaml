# add common resources
{{ range $path, $_ := .Files.Glob "common/*" }}
{{ tpl ($.Files.Get $path) $ }}
---
{{ end }}
# add specific resources for env
{{- $env := (printf "%s%s" $.Values.ENV_NAME "/*") -}}
{{ range $path, $_ := .Files.Glob $env }}
{{ tpl ($.Files.Get $path) $ }}
---
{{ end }}